# Live evidence research and editorial generation
OPENAI_API_KEY=
CONTENT_FACTORY_MODEL=gpt-5.4-mini

# Optional override. imageio-ffmpeg supplies a bundled binary by default.
FFMPEG_PATH=

# Future publishing adapters intentionally remain draft-only until configured.
YOUTUBE_CLIENT_SECRETS=
HEYGEN_API_KEY=
